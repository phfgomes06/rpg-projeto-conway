#include "dialogos.h"
#include "main-enredo.h"

void comecarJogo() {
    int input;
    lore::inicio();
    input = exibirOpcoes("O que vocês fazem?", {"Investigar os arbustos", "Ignorar"});
    if (input == 1) {
        
    }
}