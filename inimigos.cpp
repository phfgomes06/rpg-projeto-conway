#include "construtor-fichas/structs.h"
#include <vector>
using namespace std;

namespace inimigo {
    void onca(vector<Ficha*> inimigos) {
        Ficha* f = new Ficha();
        f->nome = "Onça";
        f->classe = nenhuma_classe;
        f->arma = 
    }
}